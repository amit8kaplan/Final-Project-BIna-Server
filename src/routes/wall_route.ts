// import express from 'express';
// const router = express.Router();
// import dapit_Controller from '../controllers/dapit_Controller';
// // import wall_Controller from '../controllers/wall_controller.ts';
// import authMiddleware from '../common/auth_middleware';
// import { upload_vid } from '../common/file_upload';
// import exp from 'constants';

// //get the wall of the triner
// router.get(('/:trinerId'), wall_Controller.get.bind(wall_Controller));

// //get the wall of the triner - but with filters
// router.get(('/:trinerId/getByFilter'), wall_Controller.getByFilter.bind(wall_Controller));